gpu_configs:
  dram_bw: 3352 # GB/s, 80GB SXM
  dram_freq: 2619 # MHz (check with nvidia-smi -q -d CLOCK)
  cuda_fp64_flops: 33.5 # TFLOPs, CUDA
  cuda_fp32_flops: 66.9 # TFLOPs, CUDA
  tc_fp64_flops: 66.9 # TFLOPs, TensorCore
  tc_fp32_flops: 494.7 # TFLOPs, TensorCore
  tc_bf16_flops: 989.4 # TFLOPs, TensorCore
  tc_fp16_flops: 989.4 # TFLOPs, TensorCore
  tc_int8_flops: 1978.9 # TFLOPs, TensorCore
  sm_freq: 1410 # MHz, If not sure, check the maximum frequency with nvidia-smi -q -d CLOCK
  sm_max_freq: 1830 #Boosted maximum frequency where the TFLOPs were reported
  num_sm: 132
  dram_size: 80 # GB
  power_cap: 700 # W
  l2_size: 50 # MB
  l2_bw: 5120 # Bytes/cycle (check Whitepaper)
  xu_bw_per_sm: 16 # 16 instructions per cycle throughput per SM